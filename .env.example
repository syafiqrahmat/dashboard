DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

MYSUPPORT_DB_HOST=
MYSUPPORT_DB_PORT=3306
MYSUPPORT_DB_NAME=mysupport
MYSUPPORT_DB_USER=
MYSUPPORT_DB_PASSWORD=

# Set the same value here and as a Vercel project env var to authenticate
# the scheduled sync (see vercel.json "crons" + require_cron_or_admin()).
CRON_SECRET=
